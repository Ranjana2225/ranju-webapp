- hosts: localhost
  become: yes
  gather_facts: false
  tasks:
    - name: Copy WAR file to Tomcat webapps directory
      copy:
        src: target/ranju-webapp.war
        dest: /opt/tomcat2/webapps/ranju-webapp.war
